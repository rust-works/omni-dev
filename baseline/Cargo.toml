[package]
name = "baseline"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "baseline"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ulid = { version = "1", features = ["serde"] }
