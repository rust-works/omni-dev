# Pinned to the exact versions used for the #873 measurements.

# Offline path (run.py): MLX-accelerated Moonshine via mlx-audio.
# Note: mlx-audio's Moonshine generate() is offline-only and has no
# streaming API; the model's encoder also caps at ~30 s input (max
# position_embeddings = 512), so the harness chunks the audio.
mlx-audio==0.4.3

# Streaming path (run_streaming.py): canonical upstream Moonshine package.
# Ships the streaming-trained variants (Tiny/Base/Small/Medium Streaming)
# and exposes a Transcriber(add_audio, add_listener) API. ONNX/CoreML-
# backed on macOS, not MLX. See README.md for the runtime caveat.
moonshine-voice==0.0.59

# Shared
soundfile==0.13.1
numpy==2.4.6
python-ulid==3.1.0
