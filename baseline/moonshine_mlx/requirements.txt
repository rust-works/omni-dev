# Pin to exact versions after first successful install on the spike host;
# leave as floors / TBD until then.

# Offline path (run.py): MLX-accelerated Moonshine via mlx-audio.
mlx-audio>=0.2

# Streaming path (run_streaming.py): upstream streaming-trained Moonshine.
# The exact package name and version need to be confirmed against the
# moonshine-ai/moonshine repo at install time — the streaming variants
# ship via `useful-moonshine` or are loaded directly from HF
# (`UsefulSensors/moonshine-streaming-medium`) with the `transformers`
# library. Adjust the requirement below to match whatever the upstream
# README points at.
useful-moonshine>=0.1
transformers>=4.46
torch>=2.2

# Shared
soundfile~=0.13
numpy>=2.0,<3
python-ulid~=3.0
huggingface_hub>=0.25
